services:
  - type: web
    name: investor-scout
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn ui:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
